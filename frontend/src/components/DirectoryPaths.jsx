// This shows the directory paths that Clawbot is currently using
// and where it has been
function DirectoryPaths() {
    return  (
        <div>
            <Form />
        </div>
    );
}

export default DirectoryPaths;