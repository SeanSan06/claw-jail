// This will show the current status of Clawbot, such as whether it's idle, 
// executing a command, or if there are any errors. It can also display 
// the current command being executed and its progress.
function StatusIndicator() {
    return  (
        <div>
            <Form />
        </div>
    );
}

export default StatusIndicator;