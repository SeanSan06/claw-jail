{
  "id": "tool-call-logger",
  "name": "Tool Call Logger",
  "description": "Intercepts all agent tool calls and prints them to the console",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "webhookUrl": {
        "type": "string",
        "description": "HTTP URL to POST tool call events to (overrides TOOL_CALL_WEBHOOK_URL env var)"
      }
    }
  },
  "uiHints": {
    "webhookUrl": {
      "label": "Webhook URL",
      "help": "HTTP endpoint that receives full tool call event payloads as JSON POST requests"
    }
  }
}
